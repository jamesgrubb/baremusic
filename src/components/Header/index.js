import React from 'react'
import * as Styled from './Styles'

export default () => (<Styled.Header as="header" left={[3,4]} position="absolute" top={[3,4]}>
    <Styled.Title><span>Bare</span><span>Music</span></Styled.Title>
</Styled.Header>)